---
import Layout from '../layouts/Layout.astro';
import { App } from '../components/App';
import config from '../data/config.json';
---

<Layout title={config.siteMeta.title}>
  <App client:load siteMeta={config.siteMeta} />
</Layout>
